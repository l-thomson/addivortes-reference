% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{predictive_qq}
\alias{predictive_qq}
\title{Predictive-QQ PIT values}
\usage{
predictive_qq(y, fit_draws, s_draws)
}
\arguments{
\item{y}{Observed response, length \code{n_rows}.}

\item{fit_draws}{\code{n_draws} x \code{n_rows} matrix of per-draw fits
(\code{predict(fit, x, type = "draws")}).}

\item{s_draws}{\code{n_draws} x \code{n_rows} matrix of per-draw error SDs
(strictly positive), e.g. \code{matrix(sigma(fit), n_draws, n_rows)}.}
}
\value{
Sorted PIT values, length \code{n_rows}.
}
\description{
PIT values (sorted ascending) of the heteroscedastic Gaussian predictive
\code{N(fit_d(x_i), s_d(x_i)^2)} against observed \code{y}. Plot against uniform
quantiles \code{(i - 1/2) / n}: a straight line is a calibrated model.
}
