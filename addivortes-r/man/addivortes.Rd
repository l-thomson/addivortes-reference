% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/addivortes.R
\name{addivortes}
\alias{addivortes}
\title{Fit an AddiVortes model}
\usage{
addivortes(
  x,
  y,
  seed,
  m = NULL,
  burn_in = NULL,
  draws = NULL,
  thinning = NULL,
  nu = NULL,
  q = NULL,
  k = NULL,
  lambda_c = NULL,
  omega = NULL,
  sigma_c = NULL,
  metrics = NULL,
  moves = NULL,
  coords = NULL,
  distance = NULL,
  inclusion = NULL,
  scale = NULL,
  count_priors = NULL,
  basis = NULL,
  membership = NULL,
  response_family = c("gaussian", "binary_probit", "robust_t"),
  t_df = NULL,
  chains = 1L
)
}
\arguments{
\item{x}{Numeric predictor matrix (or something \code{\link[=as.matrix]{as.matrix()}} turns into
one), one row per observation, raw caller scale.}

\item{y}{Numeric response of length \code{nrow(x)}; for
\code{response_family = "binary_probit"} the values must be 0/1.}

\item{seed}{RNG seed (a non-negative whole number). Mandatory, as in the
Rust and Python interfaces: two callers who omit it would otherwise
share a chain and could mistake that for independent replication.}

\item{m}{Number of tessellations in the ensemble.}

\item{burn_in}{Number of burn-in sweeps to discard.}

\item{draws}{Number of posterior draws to keep.}

\item{thinning}{Keep every \code{thinning}-th post-burn-in draw.}

\item{nu, q}{Error-variance prior: degrees of freedom and quantile.}

\item{k}{Terminal-output prior spread.}

\item{lambda_c}{Poisson prior rate for cell counts.}

\item{omega}{Poisson prior rate for active dimensions (must be below the
number of features).}

\item{sigma_c}{Coordinate proposal spread.}

\item{metrics}{Per-column metric names (\code{"euclidean"}, \code{"spherical"},
\code{"categorical"}, \code{"prepared"}), or \code{NULL} for all-Euclidean.}

\item{moves}{Structural moves and their selection weights: a list
of \code{list(name =, weight =)}, or \code{NULL} for the paper's six-move set.}

\item{coords}{Centre-coordinate laws, one per raw column: a list
of \code{list(type = "euclidean_normal", sigma_c =)} or \code{"wrapped_normal"}.
\code{NULL} uses the per-metric defaults at \code{sigma_c}.}

\item{distance}{Assignment geometry: a kind name (\code{"euclidean"},
\code{"manhattan"}, \code{"cosine"}, \code{"spherical"}) or an \code{\link[=avt_distance]{avt_distance()}}
specification for the parameterised kinds (\code{"minkowski"}, \code{"gower"},
\code{"mahalanobis"}). \code{NULL} uses the paper's per-column geometry.}

\item{inclusion}{Covariate-inclusion model: e.g.
\code{list(type = "dart", alpha = 0.5)},
\code{list(type = "weighted", weights = c(...))}, or \code{NULL} for uniform.}

\item{scale}{Error-variance model: e.g.
\code{list(type = "h_variance", m_prime = 40)} for the heteroscedastic
ensemble, or \code{list(type = "pinned", sigma_sq = 1)}. \code{NULL} uses the
engine's calibrated global sigma.}

\item{count_priors}{Cell/dimension count priors; \code{NULL} uses the
paper's pair.}

\item{basis}{Within-cell basis: e.g.
\code{list(type = "linear", columns = c(0), sigma_beta_sq = 0.1)} for local
linear cells. \code{NULL} gives a constant per cell.}

\item{membership}{Soft cell membership: an \code{\link[=avt_membership]{avt_membership()}}
specification, a named list, or \code{NULL} (the default) for hard
membership. Note models with a non-default extension point refuse JSON
serialisation, with the engine's own message.}

\item{response_family}{\code{"gaussian"} (default), \code{"binary_probit"}
(predictions on the probability scale), or \code{"robust_t"} (requires
\code{t_df}).}

\item{t_df}{Student-t degrees of freedom; only with
\code{response_family = "robust_t"}.}

\item{chains}{Number of independent chains (seeds derived from \code{seed};
chain 1 is bit-identical to \code{chains = 1}).}
}
\value{
For \code{chains = 1} an object of class \code{addivortes_fit}; otherwise
an \code{addivortes_chains} list of such fits. See
\code{\link[=predict.addivortes_fit]{predict.addivortes_fit()}}, \code{\link[=prediction_interval]{prediction_interval()}},
\code{\link[=log_likelihood]{log_likelihood()}}, \code{\link[=sigma.addivortes_fit]{sigma.addivortes_fit()}}, \code{\link[=total_cells]{total_cells()}},
\code{\link[=variable_importance]{variable_importance()}}, \code{\link[=avt_save]{avt_save()}}.
}
\description{
Bayesian additive Voronoi tessellation regression (Stone & Gosling 2025,
JCGS): the response is modelled as a sum of \code{m} Voronoi tessellations
explored by a Gibbs backfitting sampler. This function wraps the
\code{addivortes} Rust engine; hyperparameters left \code{NULL} use the engine's
defaults, which live in the crate. The engine's \code{omega} default (3.0)
requires more than 3 features; pass e.g. \code{omega = 1.5} for narrower
designs.
}
\details{
Chains are reproducible bit-for-bit given the same seed, engine version,
and compilation target (the testthat suite pins a fit against the same
golden vectors the Rust tests use).
}
\examples{
x <- matrix(stats::runif(120), 60, 2)
y <- 2 * x[, 1] - x[, 2] + stats::rnorm(60, sd = 0.1)
fit <- addivortes(x, y, seed = 1, m = 8, burn_in = 20, draws = 30, omega = 1.5)
predict(fit, x[1:3, ])
}
\references{
Stone, A. and Gosling, J.P. (2025). AddiVortes: (Bayesian)
Additive Voronoi Tessellations. JCGS 34(3), 859-871.
}
