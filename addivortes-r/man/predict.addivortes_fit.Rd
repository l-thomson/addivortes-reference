% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-methods.R
\name{predict.addivortes_fit}
\alias{predict.addivortes_fit}
\title{Predict from an AddiVortes fit}
\usage{
\method{predict}{addivortes_fit}(
  object,
  newdata,
  type = c("mean", "draws", "quantiles"),
  probs = c(0.05, 0.5, 0.95),
  ...
)
}
\arguments{
\item{object}{An \code{addivortes_fit} from \code{\link[=addivortes]{addivortes()}}.}

\item{newdata}{Numeric matrix of predictors (a single observation may be
passed as a plain vector).}

\item{type}{\code{"mean"} (default) for posterior-mean predictions, one per
row: response scale for gaussian/robust_t, probability scale for
binary_probit; \code{"draws"} for the full per-draw prediction matrix
(\code{n_draws} x \code{n_rows}), whose column means are \code{"mean"}; or
\code{"quantiles"} for posterior-predictive quantiles at \code{probs}
(\code{n_rows} x \code{length(probs)}).}

\item{probs}{Quantile probabilities, for \code{type = "quantiles"}.}

\item{...}{Unused.}
}
\value{
A vector (\code{"mean"}) or matrix (\code{"draws"}, \code{"quantiles"}).
Prediction is deterministic (it consumes no RNG).
}
\description{
Predict from an AddiVortes fit
}
