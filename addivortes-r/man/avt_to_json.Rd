% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-methods.R
\name{avt_to_json}
\alias{avt_to_json}
\alias{avt_from_json}
\alias{avt_save}
\alias{avt_load}
\title{Serialise a fit to the engine's validated JSON format}
\usage{
avt_to_json(object)

avt_from_json(json)

avt_save(object, path)

avt_load(path)
}
\arguments{
\item{object}{An \code{addivortes_fit}.}

\item{json}{A string from \code{\link[=avt_to_json]{avt_to_json()}}.}

\item{path}{File path for the JSON payload.}
}
\value{
A JSON string.
}
\description{
The payload round-trips with bit-identical predictions
(\code{avt_from_json()} / \code{avt_load()} re-validate on load, so a corrupt
payload is an error, never a crash). Models whose config carries a
non-default shelf selection (custom \code{distance}, soft \code{membership}) refuse
with the engine's own message. Feature names are an R-side convenience
and are not part of the payload. Note \code{saveRDS()} does not work on
fits: the model lives behind an external pointer; use these instead.
}
