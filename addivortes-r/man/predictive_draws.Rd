% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior.R
\name{predictive_draws}
\alias{predictive_draws}
\title{Posterior-predictive replicates (yrep)}
\usage{
predictive_draws(object, newdata)
}
\arguments{
\item{object}{An \code{addivortes_fit} or \code{addivortes_chains}.}

\item{newdata}{Numeric matrix of predictors.}
}
\value{
An \code{n_draws} x \code{n_rows} matrix (chains stacked row-wise,
chain-major).
}
\description{
Draws replicate responses from the posterior predictive, one row per
kept draw: the matrix bayesplot's \verb{ppc_*} functions consume as \code{yrep}.
Per draw: \code{N(fit, sigma^2)} for gaussian, location-scale Student-t for
robust_t, Bernoulli labels for binary_probit. Noise is drawn with R's
RNG: call \code{\link[=set.seed]{set.seed()}} first for reproducible replicates (the fit
itself is untouched; prediction consumes no engine RNG).
}
