% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parsnip.R
\name{addivortes_reg}
\alias{addivortes_reg}
\alias{addivortes_class}
\title{AddiVortes model specifications for parsnip}
\usage{
addivortes_reg(mode = "regression", trees = NULL, engine = "addivortes")

addivortes_class(mode = "classification", trees = NULL, engine = "addivortes")
}
\arguments{
\item{mode}{\code{"regression"} / \code{"classification"}.}

\item{trees}{Ensemble size (the engine's \code{m}).}

\item{engine}{\code{"addivortes"} (the only engine).}
}
\value{
A parsnip model specification.
}
\description{
Tidymodels front door: \code{addivortes_reg()} (regression, gaussian or
robust-t via engine arguments) and \code{addivortes_class()} (binary
classification through the probit family). One tunable main argument,
\code{trees} (the ensemble size \code{m}); every other engine hyperparameter
passes through \code{parsnip::set_engine()} (e.g.
\code{set_engine("addivortes", seed = 1, omega = 1.5)}).
}
\details{
The engines register when the package loads with parsnip available
(or as soon as parsnip is loaded afterwards).
}
\examples{
\dontrun{
library(parsnip)
spec <- addivortes_reg(trees = 20) |>
  set_engine("addivortes", seed = 1, burn_in = 100, draws = 200)
fitted <- fit(spec, y ~ ., data = my_data)
predict(fitted, my_data)
}
}
