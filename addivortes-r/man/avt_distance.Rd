% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/addivortes.R
\name{avt_distance}
\alias{avt_distance}
\title{Assignment geometry (distance) specifications}
\usage{
avt_distance(
  kind = c("euclidean", "manhattan", "cosine", "spherical", "minkowski", "gower",
    "mahalanobis"),
  p = NULL,
  columns = NULL,
  levels = NULL,
  precision = NULL
)
}
\arguments{
\item{kind}{One of \code{"euclidean"}, \code{"manhattan"}, \code{"cosine"},
\code{"spherical"}, \code{"minkowski"}, \code{"gower"}, \code{"mahalanobis"}.}

\item{p}{Minkowski order (>= 1); \code{"minkowski"} only.}

\item{columns}{For \code{"gower"}: a character vector declaring each RAW
column (in \code{metrics} order) as \code{"numeric"} or \code{"categorical"}.}

\item{levels}{For \code{"gower"}: an integer vector, the number of levels per
column (ignored for numeric columns).}

\item{precision}{For \code{"mahalanobis"}: the square precision matrix over
the ENCODED design width.}
}
\value{
An \code{addivortes_distance} specification for \code{\link[=addivortes]{addivortes()}}.
}
\description{
Selects the engine's built-in cell-assignment geometry (the distance
extension point's shelf). Plain kinds can also be passed to \code{\link[=addivortes]{addivortes()}} directly as a
string; the parameterised kinds take their parameters here. Validation
(e.g. Mahalanobis symmetry and positive definiteness) happens in the
engine's own constructors at fit time.
}
\examples{
avt_distance("minkowski", p = 3)
avt_distance("gower",
  columns = c("numeric", "categorical"),
  levels = c(0, 3)
)
}
